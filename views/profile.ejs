<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10" >
      <div class=" w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout

        </a>
      </div>
      <div class="flex items-start gap-3">
        
      <div class="w-10 h-10 rounded-md overflow-hidden">
        <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic%>" alt="">
      </div>
      <h3 class="text-3xl mb-2"><Span class="font-light">Hello</Span>, <%= user.name %> </h3>
      </div>
      <h5 class="mb-5 mt-3 ">you can create a post</h5>
          <form method="post" action="/post">
            <textarea placeholder="What's in your mind?" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="px-2 py-2 bg-yellow-500 text-sm block rounded-md mt-2 w-40" type="submit" value="Create New Post">
          </form>
      </form>
      <div class="mt-20"><h3 class="text-xl text-zinc-400 mb-2">Your Posts</h3>
        <div class="postcontainer">
         <% user.post.reverse().forEach(function(post){%>
              <div class="post w-1/3 p-4 mb-4 border-2 bg-zinc-800 rounded-md border-zinc-700">
            <h4 class=" text-blue-400 mb-2">@<%= user.username %> </h4>
              <p class="text-sm tracking-tight mb-2"><%= post.content %></p>
              <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
              <div class="btns flex gap-4 mt-1">
              <a class="text-blue-500 " href="/like/<%= post._id %>">Like</a>
              <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit </a>

            </div>
          </div>
          <%}) %>
        </div>
      </div>
    </div>  
  </body>
</html>
